pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
				cd myproject
				mvn compile
            }
        }
		stage('Test') {
			steps{
				mvn test
			}
		}
		stage('Deploy') {
			steps{
				mvn package
			}
		}
    }
}